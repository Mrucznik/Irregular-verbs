<Window x:Class="IrregularVerbs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IrregularVerbs"
        mc:Ignorable="d"
        Title="MainWindow" Height="174.924" Width="310.957">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Grid.Column="0" x:Name="CheckButton" Content="Learn" Click="CheckButton_Click" BorderBrush="{x:Null}" Background="#FF2A5C97"/>
        <Button Grid.Column="1" x:Name="AddButton" Content="Add verbs" Click="AddButton_Click" BorderBrush="{x:Null}" Background="#FF2A5C97"/>
    </Grid>
</Window>
